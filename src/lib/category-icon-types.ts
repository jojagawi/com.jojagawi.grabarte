export type CategoryIconOption = {
  value: string;
  label: string;
};

export type CategoryIconPackMeta = {
  id: string;
  name: string;
};

export type CategoryIconPack = {
  id: string;
  name: string;
  options: CategoryIconOption[];
};

